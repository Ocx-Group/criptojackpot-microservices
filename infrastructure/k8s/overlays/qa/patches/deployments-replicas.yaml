# Replicas and resources for QA deployments
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
            requests:
          resources:
        - name: "*"
      containers:
    spec:
  template:
  replicas: 2
spec:
  name: not-important
metadata:
kind: Deployment
apiVersion: apps/v1
