# BFF Gateway Service
apiVersion: v1
kind: Service
metadata:
  name: bff-gateway
  namespace: criptojackpot
  labels:
    app: bff-gateway
    component: api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: bff-gateway

