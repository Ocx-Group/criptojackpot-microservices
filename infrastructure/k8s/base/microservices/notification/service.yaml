# Notification API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: criptojackpot
  labels:
    app: notification-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: notification-api
