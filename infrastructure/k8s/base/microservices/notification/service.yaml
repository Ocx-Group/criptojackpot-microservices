# Notification API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: cryptojackpot
  labels:
    app: notification-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: notification-api
