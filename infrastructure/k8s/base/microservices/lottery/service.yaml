# Lottery API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: lottery-api
  namespace: criptojackpot
  labels:
    app: lottery-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: lottery-api
