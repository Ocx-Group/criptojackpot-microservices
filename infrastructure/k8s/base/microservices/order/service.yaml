# Order API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: order-api
  namespace: cryptojackpot
  labels:
    app: order-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: order-api
