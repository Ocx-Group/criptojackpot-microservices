# Order API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: order-api
  namespace: criptojackpot
  labels:
    app: order-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: order-api
