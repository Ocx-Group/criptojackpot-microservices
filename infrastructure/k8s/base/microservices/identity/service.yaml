# Identity API - Base Service
apiVersion: v1
kind: Service
metadata:
  name: identity-api
  namespace: criptojackpot
  labels:
    app: identity-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: identity-api
